{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Default capabilities for the MindSync Tauri shell (M1: window only)",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
