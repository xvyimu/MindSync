# 90 天执行 Backlog（本地工作台做深）

> 依据：竞品调研 §18 / §36 + 决策简报 `COMPETITIVE-BRIEF.md` + FULL-SCAN 残余债。  
> **主目标**：闭环与可维护，不平台化。  
> 状态列：`todo` / `doing` / `done` / `wont`。

| 项 | 值 |
|----|-----|
| 起点 | 2026-07-21 |
| 代码基线 | `develop` @ **以 `git log -1` 为准**（A5/C1/C2 已绿；C3/手测/导出确认本刀） |
| 硬约束 | 本地优先 · fork-only · 包边界 · 小核心 · **导出默认脱敏** · **Web 无 S3 SDK** |
| 决策 | 表单冻结见 `COMPETITIVE-BRIEF.md`；整合调研 `INTEGRATED-WORKBENCH-DECISION-RESEARCH-2026-07-21.md` |

---

## 阶段 A（第 1–2 周）：边界冻结与巩固

| ID | 项 | 验收（是/否） | 状态 | 备注 |
|----|-----|---------------|------|------|
| A0 | 远端 develop 与本地 tip 对齐 | `git status` 无 ahead | **done** | 2026-07-21 push 至 `ac3328f`（含 F2/CI 修复链） |
| A1 | UI 删除 core 工厂 re-export | package-scripts 测试绿 | **done** | `be0a5de` |
| A2 | web/extension 直依 core + 生产 dist alias | 契约测试绿 | **done** | `ceccc57` |
| A3 | CURRENT 链接调研与扫描规划 | 文档入口可点 | **done** | 含整合调研链接 |
| A4 | 宪章 + 简报 + 本 backlog + 下一刀规格落盘 | 四文件存在并互链 | **done** | + RELEASE-F2 规格 |
| A5 | CI 全绿（gate + lint + mcp + e2e） | Actions 成功 | **done** | tip `e21e7fd` run [29802914336](https://github.com/xvyimu/MindSync/actions/runs/29802914336) success |
| B1 | **EvalCaseSet 最小类型 + 本地存取** | 见 NEXT-CUT-SPEC | **done** | core+UI Cut-1 |
| B2 | 评估跑批最小路径 + 证据 JSON 导出 | 手测 + 单测 | **done** | 单测 13；UI BasicSystem |

**阶段 A 出门条件**：A0–A4 = done；A5 无红。

---

## 阶段 B（第 3–5 周）：闭环数据与安全

| ID | 项 | 验收 | 状态 | 依赖 |
|----|-----|------|------|------|
| B1 | **EvalCaseSet 最小类型 + 本地存取** | 见 NEXT-CUT-SPEC | **done** | core+UI；手测修补 |
| B2 | 评估跑批最小路径 + 证据 JSON 导出 | 手测 + 单测 | **done** | 单测 13；UI 错误展示/表单不清空 |
| B3 | 历史上限可感知（警告/配置） | 超限有 UI 提示 | **done** | getUsage + HistoryDrawer 警告/可调上限 |
| B4 | Desktop **safeStorage** 包装 API Key 字段 | Key 不以明文偏好存储 | **done** | SecretAwareStorage + Electron safeStorage |
| B5 | 远程备份：**UI 去掉 @aws-sdk** 或 Desktop-only 硬门闩 | web bundle 无 aws-sdk 或明确禁用 | **done** | UI 去 @aws-sdk；Web 仅 Google Drive；S3/R2/WebDAV Desktop IPC |
| B6 | 路径规范化回归测试（已对齐规则） | 单测覆盖 `..`/控制字符 | **done** | UI `normalize-object-path.spec` + desktop remote-storage 测 |
| F2 | **EvalCase 进全量导出/导入** | 导出含用例；导入可跑 | **done** | `exportAllData` 键 `evalCaseSets`；Cut-R1 |

**阶段 B 出门条件**：B1+B2 完成；B4 或书面风险接受；导出含 favorites（已有方向）+ 证据。

---

## 阶段 C（第 6–9 周）：评测互操作（服务主目标，非切主目标）

| ID | 项 | 验收 | 状态 | 说明 |
|----|-----|------|------|------|
| C1 | 双模型对照轻量 UI | 同提示两 modelKey 并排 | **done** | 一键「双模型」+ 每列 modelKey；seed 纯函数；Basic System/User |
| C2 | 导出 `promptfoo.yaml` 适配器 | 文档级最小集可跑 | **done** | `7509047`+`e21e7fd` barrel；EvalCase 面板导出；单测+CI 绿 |
| C3 | MCP 工具结构化返回（original/optimized/meta） | schema 稳定 | **done** | `structured-result.ts` + 三工具 JSON 返回；单测 |
| C4 | 优化完成 CTA 固定：测试/评估/收藏 | 手测三端至少 Web+Desktop | **done** | PostOptimizeActions · Basic System/User |

**阶段 C 出门条件**：C2 或 C1 至少一项可用；C4 完成。

---

## 阶段 D（第 10–12 周）：可选进取（默认关）

| ID | 项 | 验收 | 状态 | 约束 |
|----|-----|------|------|------|
| D1 | `OptimizationStrategy` 接口草落地 | 默认 Template 策略 | **done** | `optimization-strategy.ts`；默认 template | |
| D2 | 实验开关「自动优化」+ 预算 + 取消 | 默认关 | **done** | pref + normalize；**默认 enabled=false**；取消由调用方 Abort | |
| D3 | ServiceContainer 继续瘦 main | main 无业务装配细节 | **done** | `createCoreServices` 内聚 core/preference；`register-domain-handlers` |
| D4 | 非 root Docker 里程碑 | 文档+镜像 | **done** | uid 10001 `app`；MCP `user=app`；高端口文档示例 |

**阶段 D 出门条件**：D1–D2 若做则必须默认关；不做可标 `wont` 并写原因。

---

## 阶段 E（R3 · 可交付，90d A–D 之后）

| ID | 项 | 验收 | 状态 | 备注 |
|----|-----|------|------|------|
| E0 | 真机手测清单签字 | HANDTEST §1–5 | **todo** | 真机勾选 |
| E1 | 入口铺全 CTA+Eval+Dual | Context+Basic User | **done**（代码） | 手测待 E0 |
| E2 | 发版/NSIS | 安装包+CURRENT | **todo** | 建议 E0 后 |
| E3 | 手测缺陷 | 回归 | 按需 | |

规格：`NEXT-CUT-SPEC-2026-07-21-E0-E1-E2.md`

---

## 明确 Won't（90 天内）

| 项 | 原因 |
|----|------|
| 多租户 / 计费 | 非工作台目标 |
| 生产 Trace 云 | 维护与隐私冲突 |
| 通用 Agent OS | 应做 MCP 工具而非宿主 |
| 默认 K8s | 单人运维 |
| 整仓 Promptfoo vendor | 互导出即可 |
| 默认自动优化开启 | 成本与可解释性 |

---

## 每周节奏（提醒）

调研 §26：周一安全 CI · 周二核心路径 · 周三闭环 · 周四边界债 · 周五文档。  
每次竞品学习只允许带回 **一个** backlog 项。

---

## 状态更新规则

- 完成项：改状态为 `done` 并在 PR/commit 引用 ID（如 `B1`）。  
- 放弃项：`wont` + 一行原因。  
- 不在此表的大功能：先改 COMPETITIVE-BRIEF，再开 ID。
