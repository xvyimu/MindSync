# 90 天执行 Backlog（本地工作台做深）

> 依据：竞品调研 §18 / §36 + 决策简报 `COMPETITIVE-BRIEF.md` + FULL-SCAN 残余债。  
> **主目标**：闭环与可维护，不平台化。  
> 状态列：`todo` / `doing` / `done` / `wont`。

| 项 | 值 |
|----|-----|
| 起点 | 2026-07-21 |
| 代码基线 | `develop` @ `be0a5de+` |
| 硬约束 | 本地优先 · fork-only · 包边界 · 小核心 |

---

## 阶段 A（第 1–2 周）：边界冻结与巩固

| ID | 项 | 验收（是/否） | 状态 | 备注 |
|----|-----|---------------|------|------|
| A0 | 远端 develop 与本地 tip 对齐 | `git status` 无 ahead | **done** | 已 push 至 `be0a5de` |
| A1 | UI 删除 core 工厂 re-export | package-scripts 测试绿 | **done** | `be0a5de` |
| A2 | web/extension 直依 core + 生产 dist alias | 契约测试绿 | **done** | `ceccc57` |
| A3 | CURRENT 链接调研与扫描规划 | 文档入口可点 | **done** | |
| A4 | 宪章 + 简报 + 本 backlog + 下一刀规格落盘 | 四文件存在并互链 | **done** | `12e3c52` |
| A5 | CI 全绿（gate + lint + mcp:test） | Actions 成功 | todo | push 后观察 |
| B1 | **EvalCaseSet 最小类型 + 本地存取** | 见 NEXT-CUT-SPEC | **done** | core+UI Cut-1 |
| B2 | 评估跑批最小路径 + 证据 JSON 导出 | 手测 + 单测 | **done** | 单测 13；UI BasicSystem |

**阶段 A 出门条件**：A0–A4 = done；A5 无红。

---

## 阶段 B（第 3–5 周）：闭环数据与安全

| ID | 项 | 验收 | 状态 | 依赖 |
|----|-----|------|------|------|
| B1 | **EvalCaseSet 最小类型 + 本地存取** | 见 NEXT-CUT-SPEC | todo | A |
| B2 | 评估跑批最小路径 + 证据 JSON 导出 | 手测 + 单测 | todo | B1 |
| B3 | 历史上限可感知（警告/配置） | 超限有 UI 提示 | todo | — |
| B4 | Desktop **safeStorage** 包装 API Key 字段 | Key 不以明文偏好存储 | todo | — |
| B5 | 远程备份：**UI 去掉 @aws-sdk** 或 Desktop-only 硬门闩 | web bundle 无 aws-sdk 或明确禁用 | todo | 宪章 §3 |
| B6 | 路径规范化回归测试（已对齐规则） | 单测覆盖 `..`/控制字符 | todo | 已有实现对齐 |

**阶段 B 出门条件**：B1+B2 完成；B4 或书面风险接受；导出含 favorites（已有方向）+ 证据。

---

## 阶段 C（第 6–9 周）：评测互操作（服务主目标，非切主目标）

| ID | 项 | 验收 | 状态 | 说明 |
|----|-----|------|------|------|
| C1 | 双模型对照轻量 UI | 同提示两 modelKey 并排 | todo | 学 Promptfoo 矩阵之浅 |
| C2 | 导出 `promptfoo.yaml` 适配器 | 文档级最小集可跑 | todo | 互操作，不 vendor |
| C3 | MCP 工具结构化返回（original/optimized/meta） | schema 稳定 | todo | 服务 IDE 嵌入，非主目标切换 |
| C4 | 优化完成 CTA 固定：测试/评估/收藏 | 手测三端至少 Web+Desktop | todo | 产品闭环 |

**阶段 C 出门条件**：C2 或 C1 至少一项可用；C4 完成。

---

## 阶段 D（第 10–12 周）：可选进取（默认关）

| ID | 项 | 验收 | 状态 | 约束 |
|----|-----|------|------|------|
| D1 | `OptimizationStrategy` 接口草落地 | 默认 Template 策略 | todo | 默认人工 |
| D2 | 实验开关「自动优化」+ 预算 + 取消 | 默认关 | todo | ADR-005 |
| D3 | ServiceContainer 继续瘦 main | main 无业务装配细节 | todo | 已有雏形 |
| D4 | 非 root Docker 里程碑 | 文档+镜像 | todo | 运维 |

**阶段 D 出门条件**：D1–D2 若做则必须默认关；不做可标 `wont` 并写原因。

---

## 明确 Won't（90 天内）

| 项 | 原因 |
|----|------|
| 多租户 / 计费 | 非工作台目标 |
| 生产 Trace 云 | 维护与隐私冲突 |
| 通用 Agent OS | 应做 MCP 工具而非宿主 |
| 默认 K8s | 单人运维 |
| 整仓 Promptfoo vendor | 互导出即可 |
| 默认自动优化开启 | 成本与可解释性 |

---

## 每周节奏（提醒）

调研 §26：周一安全 CI · 周二核心路径 · 周三闭环 · 周四边界债 · 周五文档。  
每次竞品学习只允许带回 **一个** backlog 项。

---

## 状态更新规则

- 完成项：改状态为 `done` 并在 PR/commit 引用 ID（如 `B1`）。  
- 放弃项：`wont` + 一行原因。  
- 不在此表的大功能：先改 COMPETITIVE-BRIEF，再开 ID。
