产品 MindSync。先读 docs/PROJECT.md、docs/adr/0001-tauri-desktop-mainline.md、docs/ops/ms-tauri-cutover-plan.md、docs/ops/ms-tauri-facade-progress.md、packages/desktop/config/safe-storage-secrets.js、packages/desktop/config/service-container.js。

栈：Vue3/Naive/core 不变。禁：push、删 Electron、换 UI、空口完成。验证写 docs/ops/ms-tauri-secrets-progress.md。

任务 M3 ms-tauri-secrets（orch task_33599d65a63a）：
实现 safeStorage 等价 codec 设计+可测实现，供 Tauri/desktop 后续挂接。
1) 抽象 SecretCodec 接口（encrypt/decrypt/isAvailable），与 createElectronSafeStorageCodec 对齐语义。
2) Windows：优先 DPAPI/keyring 方案（可用 Rust crate 或 Node 可选依赖，写清取舍）；不可用时行为与 Electron 一致（警告，不伪装已加密）。
3) 兼容读取 __enc:v1: 前缀策略：能读旧 blob 或文档化迁移（导出再导入）；单测 mock round-trip。
4) 改动集中 packages/core 或 packages/desktop-tauri 适配层；勿拆 Electron 主路径。
5) 禁止 push。

验收：单测 exit code；progress 写与 service-container 对接步骤 + 风险。